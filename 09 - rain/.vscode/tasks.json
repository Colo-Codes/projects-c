{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "build rain",
            "type": "shell",
            "command": "gcc", // Compiler
            "args": [
                "-g", // Global
                "-o", // Object
                "rain", // Executable
                "rain.c" // Source file(s)
            ],
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}